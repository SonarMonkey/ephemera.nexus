# The URL the site will be built for
base_url = "https://ephemera.nexus"

# The site title and description; used in feeds by default.
title = "Ephemera Nexus"
description = "A funky little website with a blog and some tutorials for making the internet better."

# The default language; used in feeds.
default_language = "en"

# When set to "true", a feed is automatically generated.
generate_feeds = true

# The filenames to use for the feeds. Used as the template filenames, too.
# Defaults to ["atom.xml"], which has a built-in template that renders an Atom 1.0 feed.
# There is also a built-in template "rss.xml" that renders an RSS 2.0 feed.
feed_filenames = ["atom.xml"]

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# The default author for pages
author = "SignPaintr4evr"

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

# The theme to use for code highlighting.
# See below for list of allowed values.
highlight_theme = "base16-ocean-dark"

# When set to "true", emoji aliases translated to their corresponding
# Unicode emoji equivalent in the rendered Markdown files. (e.g.: :smile: => 😄)
render_emoji = true

# Whether smart punctuation is enabled (changing quotes, dashes, dots in their typographic form)
# For example, `...` into `…`, `"quote"` into `“curly”` etc
smart_punctuation = true

# Whether footnotes are rendered in the GitHub-style (at the bottom, with back references) or plain (in the place, where they are defined)
bottom_footnotes = true

[extra]
# Put all your custom variables here
[[extra.nav.link]]
name = "Home"
path = '@/_index.md'
[[extra.nav.link]]
name = "Blog"
path = '@/blog/_index.md'
[[extra.nav.link]]
name = "Wiki"
path = '@/wiki/_index.md'
[[extra.nav.link]]
name = "About"
path = '@/about/_index.md'

[[extra.badge.link]]
alt = "css is difficult"
path = '@/badges/cssdif.gif'
link = ""
[[extra.badge.link]]
alt = "bookmark this page"
path = '@/badges/bookmark_this_page.gif'
link = ""
[[extra.badge.link]]
alt = "made for dark mode"
path = '@/badges/dark-mode.png'
link = ""
[[extra.badge.link]]
alt = "iframes suck"
path = '@/badges/frames-suck.png'
link = ""
[[extra.badge.link]]
alt = "proudly 98% javascript-free"
path = '@/badges/javascript-zero.png'
link = ""
[[extra.badge.link]]
alt = "say no to web3"
path = '@/badges/keep.webp'
link = ""
[[extra.badge.link]]
alt = "mobile-friendly site"
path = '@/badges/mobilefriendly.png'
link = ""
[[extra.badge.link]]
alt = "badges from seirdy.one"
path = '@/badges/seirdyone.gif'
link = ""
